description: "Tests that the gola server starts successfully with basic RAG configuration using simple embeddings."

command:
  - "/usr/local/bin/gola"
  - "--config"
  - "/test/gola.yaml"
  - "--server-only"

long_running:
  startup_timeout_ms: 5000

assertions:
  stderr_contains: "Starting Gola server on"