description: "Tests that the gola server fails to start when an MCP dependency is missing."

command:
  - "/usr/local/bin/gola"
  - "--config"
  - "/test/gola.yaml"
  - "--server-only"

assertions:
  exit_code: 1
  stderr_contains: "Failed to initialize MCP server"