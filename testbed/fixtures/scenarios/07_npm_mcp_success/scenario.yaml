description: "Tests that the gola server starts successfully when npm/npx dependencies are available."

command:
  - "/usr/local/bin/gola"
  - "--config"
  - "/test/gola.yaml"
  - "--server-only"

long_running:
  startup_timeout_ms: 10000

assertions:
  stderr_contains: "Successfully connected to MCP server: filesystem"